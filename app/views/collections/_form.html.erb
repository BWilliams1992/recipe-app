<%= render 'shared/errors', obj:@collection %>
<%= form_with(model: @collection ,class:"mt-4", local: true) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %> <br />

  <%= f.label :description %>
  <%= f.text_area :description %> <br />

  <%= f.submit %>
<% end %>