<%= form_with(url: collection_path(@collection), method: 'post',local: true) do %>
  <%= hidden_field_tag(:recipe_id, recipe.id) %>
  <%= submit_tag("Remove", class: "btn btn-lg btn-danger")  %>
<% end %>